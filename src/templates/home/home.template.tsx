import { Navbar } from 'components';

import * as s from './home.template.styles';

const HomeTemplate = () => {
  return (
    <s.Wrapper>
      <Navbar />
    </s.Wrapper>
  );
};

export default HomeTemplate;
